;**********************************************
;DO NOT MODIFY GENERATED BY make-proto-tree.scm
(declare (unit GtkSpinButton))
(define GtkSpinButton (GtkEntry 'clone))
(GtkSpinButton 'add-method-slot! 'configure (lambda (self resend  adjustment climb_rate digits) 
	(gtk-spin-button-configure (self 'this)  adjustment climb_rate digits)))
(GtkSpinButton 'add-method-slot! 'set_adjustment (lambda (self resend  adjustment) 
	(gtk-spin-button-set-adjustment (self 'this)  adjustment)))
(GtkSpinButton 'add-method-slot! 'get_adjustment (lambda (self resend ) 
	(gtk-spin-button-get-adjustment (self 'this) )))
(GtkSpinButton 'add-method-slot! 'set_digits (lambda (self resend  digits) 
	(gtk-spin-button-set-digits (self 'this)  digits)))
(GtkSpinButton 'add-method-slot! 'get_digits (lambda (self resend ) 
	(gtk-spin-button-get-digits (self 'this) )))
(GtkSpinButton 'add-method-slot! 'set_increments (lambda (self resend  step page) 
	(gtk-spin-button-set-increments (self 'this)  step page)))
(GtkSpinButton 'add-method-slot! 'get_increments (lambda (self resend  step page) 
	(gtk-spin-button-get-increments (self 'this)  step page)))
(GtkSpinButton 'add-method-slot! 'set_range (lambda (self resend  min max) 
	(gtk-spin-button-set-range (self 'this)  min max)))
(GtkSpinButton 'add-method-slot! 'get_range (lambda (self resend  min max) 
	(gtk-spin-button-get-range (self 'this)  min max)))
(GtkSpinButton 'add-method-slot! 'get_value (lambda (self resend ) 
	(gtk-spin-button-get-value (self 'this) )))
(GtkSpinButton 'add-method-slot! 'get_value_as_int (lambda (self resend ) 
	(gtk-spin-button-get-value-as-int (self 'this) )))
(GtkSpinButton 'add-method-slot! 'set_value (lambda (self resend  value) 
	(gtk-spin-button-set-value (self 'this)  value)))
(GtkSpinButton 'add-method-slot! 'set_update_policy (lambda (self resend  policy) 
	(gtk-spin-button-set-update-policy (self 'this)  policy)))
(GtkSpinButton 'add-method-slot! 'get_update_policy (lambda (self resend ) 
	(gtk-spin-button-get-update-policy (self 'this) )))
(GtkSpinButton 'add-method-slot! 'set_numeric (lambda (self resend  numeric) 
	(gtk-spin-button-set-numeric (self 'this)  numeric)))
(GtkSpinButton 'add-method-slot! 'get_numeric (lambda (self resend ) 
	(gtk-spin-button-get-numeric (self 'this) )))
(GtkSpinButton 'add-method-slot! 'spin (lambda (self resend  direction increment) 
	(gtk-spin-button-spin (self 'this)  direction increment)))
(GtkSpinButton 'add-method-slot! 'set_wrap (lambda (self resend  wrap) 
	(gtk-spin-button-set-wrap (self 'this)  wrap)))
(GtkSpinButton 'add-method-slot! 'get_wrap (lambda (self resend ) 
	(gtk-spin-button-get-wrap (self 'this) )))
(GtkSpinButton 'add-method-slot! 'set_snap_to_ticks (lambda (self resend  snap_to_ticks) 
	(gtk-spin-button-set-snap-to-ticks (self 'this)  snap_to_ticks)))
(GtkSpinButton 'add-method-slot! 'get_snap_to_ticks (lambda (self resend ) 
	(gtk-spin-button-get-snap-to-ticks (self 'this) )))
(GtkSpinButton 'add-method-slot! 'update (lambda (self resend ) 
	(gtk-spin-button-update (self 'this) )))
(GtkSpinButton 'add-method-slot! 'new (lambda (self resend  adjustment climb_rate digits) 
	(self 'set-this! (gtk-spin-button-new  adjustment climb_rate digits))))
