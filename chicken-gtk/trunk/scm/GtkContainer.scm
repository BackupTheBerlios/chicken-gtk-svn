;**********************************************
;DO NOT MODIFY GENERATED BY make-proto-tree.scm
(declare (unit GtkContainer))
(define GtkContainer (GtkWidget 'clone))
(GtkContainer 'add-method-slot! 'set_border_width (lambda (self resend  border_width) 
	(gtk-container-set-border-width (self 'this)  border_width)))
(GtkContainer 'add-method-slot! 'get_border_width (lambda (self resend ) 
	(gtk-container-get-border-width (self 'this) )))
(GtkContainer 'add-method-slot! 'add (lambda (self resend  widget) 
	(gtk-container-add (self 'this)  widget)))
(GtkContainer 'add-method-slot! 'remove (lambda (self resend  widget) 
	(gtk-container-remove (self 'this)  widget)))
(GtkContainer 'add-method-slot! 'set_resize_mode (lambda (self resend  resize_mode) 
	(gtk-container-set-resize-mode (self 'this)  resize_mode)))
(GtkContainer 'add-method-slot! 'get_resize_mode (lambda (self resend ) 
	(gtk-container-get-resize-mode (self 'this) )))
(GtkContainer 'add-method-slot! 'check_resize (lambda (self resend ) 
	(gtk-container-check-resize (self 'this) )))
(GtkContainer 'add-method-slot! 'foreach (lambda (self resend  callback callback_data) 
	(gtk-container-foreach (self 'this)  callback callback_data)))
(GtkContainer 'add-method-slot! 'foreach_full (lambda (self resend  callback marshal callback_data notify) 
	(gtk-container-foreach-full (self 'this)  callback marshal callback_data notify)))
(GtkContainer 'add-method-slot! 'get_children (lambda (self resend ) 
	(gtk-container-get-children (self 'this) )))
(GtkContainer 'add-method-slot! 'propagate_expose (lambda (self resend  child event) 
	(gtk-container-propagate-expose (self 'this)  child event)))
(GtkContainer 'add-method-slot! 'set_focus_chain (lambda (self resend  focusable_widgets) 
	(gtk-container-set-focus-chain (self 'this)  focusable_widgets)))
(GtkContainer 'add-method-slot! 'get_focus_chain (lambda (self resend  focusable_widgets) 
	(gtk-container-get-focus-chain (self 'this)  focusable_widgets)))
(GtkContainer 'add-method-slot! 'unset_focus_chain (lambda (self resend ) 
	(gtk-container-unset-focus-chain (self 'this) )))
(GtkContainer 'add-method-slot! 'set_reallocate_redraws (lambda (self resend  needs_redraws) 
	(gtk-container-set-reallocate-redraws (self 'this)  needs_redraws)))
(GtkContainer 'add-method-slot! 'set_focus_child (lambda (self resend  child) 
	(gtk-container-set-focus-child (self 'this)  child)))
(GtkContainer 'add-method-slot! 'set_focus_vadjustment (lambda (self resend  adjustment) 
	(gtk-container-set-focus-vadjustment (self 'this)  adjustment)))
(GtkContainer 'add-method-slot! 'get_focus_vadjustment (lambda (self resend ) 
	(gtk-container-get-focus-vadjustment (self 'this) )))
(GtkContainer 'add-method-slot! 'set_focus_hadjustment (lambda (self resend  adjustment) 
	(gtk-container-set-focus-hadjustment (self 'this)  adjustment)))
(GtkContainer 'add-method-slot! 'get_focus_hadjustment (lambda (self resend ) 
	(gtk-container-get-focus-hadjustment (self 'this) )))
(GtkContainer 'add-method-slot! 'resize_children (lambda (self resend ) 
	(gtk-container-resize-children (self 'this) )))
(GtkContainer 'add-method-slot! 'child_type (lambda (self resend ) 
	(gtk-container-child-type (self 'this) )))
(GtkContainer 'add-method-slot! 'add_with_properties (lambda (self resend  widget first_prop_name) 
	(gtk-container-add-with-properties (self 'this)  widget first_prop_name)))
(GtkContainer 'add-method-slot! 'child_set (lambda (self resend  child first_prop_name) 
	(gtk-container-child-set (self 'this)  child first_prop_name)))
(GtkContainer 'add-method-slot! 'child_get (lambda (self resend  child first_prop_name) 
	(gtk-container-child-get (self 'this)  child first_prop_name)))
(GtkContainer 'add-method-slot! 'child_set_valist (lambda (self resend  child first_property_name var_args) 
	(gtk-container-child-set-valist (self 'this)  child first_property_name var_args)))
(GtkContainer 'add-method-slot! 'child_get_valist (lambda (self resend  child first_property_name var_args) 
	(gtk-container-child-get-valist (self 'this)  child first_property_name var_args)))
(GtkContainer 'add-method-slot! 'child_set_property (lambda (self resend  child property_name value) 
	(gtk-container-child-set-property (self 'this)  child property_name value)))
(GtkContainer 'add-method-slot! 'child_get_property (lambda (self resend  child property_name value) 
	(gtk-container-child-get-property (self 'this)  child property_name value)))
(GtkContainer 'add-method-slot! 'forall (lambda (self resend  callback callback_data) 
	(gtk-container-forall (self 'this)  callback callback_data)))
