;**********************************************
;DO NOT MODIFY GENERATED BY make-proto-tree.scm
(declare (unit GtkLabel))
(define GtkLabel (GtkMisc 'clone))
(GtkLabel 'add-method-slot! 'set_text (lambda (self resend  str) 
	(gtk-label-set-text (self 'this)  str)))
(GtkLabel 'add-method-slot! 'get_text (lambda (self resend ) 
	(gtk-label-get-text (self 'this) )))
(GtkLabel 'add-method-slot! 'set_attributes (lambda (self resend  attrs) 
	(gtk-label-set-attributes (self 'this)  attrs)))
(GtkLabel 'add-method-slot! 'get_attributes (lambda (self resend ) 
	(gtk-label-get-attributes (self 'this) )))
(GtkLabel 'add-method-slot! 'set_label (lambda (self resend  str) 
	(gtk-label-set-label (self 'this)  str)))
(GtkLabel 'add-method-slot! 'get_label (lambda (self resend ) 
	(gtk-label-get-label (self 'this) )))
(GtkLabel 'add-method-slot! 'set_markup (lambda (self resend  str) 
	(gtk-label-set-markup (self 'this)  str)))
(GtkLabel 'add-method-slot! 'set_use_markup (lambda (self resend  setting) 
	(gtk-label-set-use-markup (self 'this)  setting)))
(GtkLabel 'add-method-slot! 'get_use_markup (lambda (self resend ) 
	(gtk-label-get-use-markup (self 'this) )))
(GtkLabel 'add-method-slot! 'set_use_underline (lambda (self resend  setting) 
	(gtk-label-set-use-underline (self 'this)  setting)))
(GtkLabel 'add-method-slot! 'get_use_underline (lambda (self resend ) 
	(gtk-label-get-use-underline (self 'this) )))
(GtkLabel 'add-method-slot! 'set_markup_with_mnemonic (lambda (self resend  str) 
	(gtk-label-set-markup-with-mnemonic (self 'this)  str)))
(GtkLabel 'add-method-slot! 'get_mnemonic_keyval (lambda (self resend ) 
	(gtk-label-get-mnemonic-keyval (self 'this) )))
(GtkLabel 'add-method-slot! 'set_mnemonic_widget (lambda (self resend  widget) 
	(gtk-label-set-mnemonic-widget (self 'this)  widget)))
(GtkLabel 'add-method-slot! 'get_mnemonic_widget (lambda (self resend ) 
	(gtk-label-get-mnemonic-widget (self 'this) )))
(GtkLabel 'add-method-slot! 'set_text_with_mnemonic (lambda (self resend  str) 
	(gtk-label-set-text-with-mnemonic (self 'this)  str)))
(GtkLabel 'add-method-slot! 'set_justify (lambda (self resend  jtype) 
	(gtk-label-set-justify (self 'this)  jtype)))
(GtkLabel 'add-method-slot! 'get_justify (lambda (self resend ) 
	(gtk-label-get-justify (self 'this) )))
(GtkLabel 'add-method-slot! 'set_ellipsize (lambda (self resend  mode) 
	(gtk-label-set-ellipsize (self 'this)  mode)))
(GtkLabel 'add-method-slot! 'get_ellipsize (lambda (self resend ) 
	(gtk-label-get-ellipsize (self 'this) )))
(GtkLabel 'add-method-slot! 'set_width_chars (lambda (self resend  n_chars) 
	(gtk-label-set-width-chars (self 'this)  n_chars)))
(GtkLabel 'add-method-slot! 'get_width_chars (lambda (self resend ) 
	(gtk-label-get-width-chars (self 'this) )))
(GtkLabel 'add-method-slot! 'set_max_width_chars (lambda (self resend  n_chars) 
	(gtk-label-set-max-width-chars (self 'this)  n_chars)))
(GtkLabel 'add-method-slot! 'get_max_width_chars (lambda (self resend ) 
	(gtk-label-get-max-width-chars (self 'this) )))
(GtkLabel 'add-method-slot! 'set_pattern (lambda (self resend  pattern) 
	(gtk-label-set-pattern (self 'this)  pattern)))
(GtkLabel 'add-method-slot! 'set_line_wrap (lambda (self resend  wrap) 
	(gtk-label-set-line-wrap (self 'this)  wrap)))
(GtkLabel 'add-method-slot! 'get_line_wrap (lambda (self resend ) 
	(gtk-label-get-line-wrap (self 'this) )))
(GtkLabel 'add-method-slot! 'set_selectable (lambda (self resend  setting) 
	(gtk-label-set-selectable (self 'this)  setting)))
(GtkLabel 'add-method-slot! 'get_selectable (lambda (self resend ) 
	(gtk-label-get-selectable (self 'this) )))
(GtkLabel 'add-method-slot! 'set_angle (lambda (self resend  angle) 
	(gtk-label-set-angle (self 'this)  angle)))
(GtkLabel 'add-method-slot! 'get_angle (lambda (self resend ) 
	(gtk-label-get-angle (self 'this) )))
(GtkLabel 'add-method-slot! 'select_region (lambda (self resend  start_offset end_offset) 
	(gtk-label-select-region (self 'this)  start_offset end_offset)))
(GtkLabel 'add-method-slot! 'get_selection_bounds (lambda (self resend  start end) 
	(gtk-label-get-selection-bounds (self 'this)  start end)))
(GtkLabel 'add-method-slot! 'get_layout (lambda (self resend ) 
	(gtk-label-get-layout (self 'this) )))
(GtkLabel 'add-method-slot! 'get_layout_offsets (lambda (self resend  x y) 
	(gtk-label-get-layout-offsets (self 'this)  x y)))
(GtkLabel 'add-method-slot! 'set_single_line_mode (lambda (self resend  single_line_mode) 
	(gtk-label-set-single-line-mode (self 'this)  single_line_mode)))
(GtkLabel 'add-method-slot! 'get_single_line_mode (lambda (self resend ) 
	(gtk-label-get-single-line-mode (self 'this) )))
(GtkLabel 'add-method-slot! 'get (lambda (self resend  str) 
	(gtk-label-get (self 'this)  str)))
(GtkLabel 'add-method-slot! 'parse_uline (lambda (self resend  string) 
	(gtk-label-parse-uline (self 'this)  string)))
(GtkLabel 'add-method-slot! 'new (lambda (self resend  str) 
	(self 'set-this! (gtk-label-new  str))))
