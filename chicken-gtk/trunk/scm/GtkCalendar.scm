;**********************************************
;DO NOT MODIFY GENERATED BY make-proto-tree.scm
(declare (unit GtkCalendar))
(define GtkCalendar (GtkWidget 'clone))
(GtkCalendar 'add-method-slot! 'select_month (lambda (self resend  month year) 
	(gtk-calendar-select-month (self 'this)  month year)))
(GtkCalendar 'add-method-slot! 'select_day (lambda (self resend  day) 
	(gtk-calendar-select-day (self 'this)  day)))
(GtkCalendar 'add-method-slot! 'mark_day (lambda (self resend  day) 
	(gtk-calendar-mark-day (self 'this)  day)))
(GtkCalendar 'add-method-slot! 'unmark_day (lambda (self resend  day) 
	(gtk-calendar-unmark-day (self 'this)  day)))
(GtkCalendar 'add-method-slot! 'clear_marks (lambda (self resend ) 
	(gtk-calendar-clear-marks (self 'this) )))
(GtkCalendar 'add-method-slot! 'set_display_options (lambda (self resend  flags) 
	(gtk-calendar-set-display-options (self 'this)  flags)))
(GtkCalendar 'add-method-slot! 'get_display_options (lambda (self resend ) 
	(gtk-calendar-get-display-options (self 'this) )))
(GtkCalendar 'add-method-slot! 'display_options (lambda (self resend  flags) 
	(gtk-calendar-display-options (self 'this)  flags)))
(GtkCalendar 'add-method-slot! 'get_date (lambda (self resend  year month day) 
	(gtk-calendar-get-date (self 'this)  year month day)))
(GtkCalendar 'add-method-slot! 'freeze (lambda (self resend ) 
	(gtk-calendar-freeze (self 'this) )))
(GtkCalendar 'add-method-slot! 'thaw (lambda (self resend ) 
	(gtk-calendar-thaw (self 'this) )))
(GtkCalendar 'add-method-slot! 'new (lambda (self resend ) 
	(self 'set-this! (gtk-calendar-new ))))
