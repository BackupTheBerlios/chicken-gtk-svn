;**********************************************
;DO NOT MODIFY GENERATED BY make-proto-tree.scm
(declare (unit GtkImage))
(define GtkImage (GtkMisc 'clone))
(GtkImage 'add-method-slot! 'clear (lambda (self resend ) 
	(gtk-image-clear (self 'this) )))
(GtkImage 'add-method-slot! 'set_from_pixmap (lambda (self resend  pixmap mask) 
	(gtk-image-set-from-pixmap (self 'this)  pixmap mask)))
(GtkImage 'add-method-slot! 'set_from_image (lambda (self resend  gdk_image mask) 
	(gtk-image-set-from-image (self 'this)  gdk_image mask)))
(GtkImage 'add-method-slot! 'set_from_file (lambda (self resend  filename) 
	(gtk-image-set-from-file (self 'this)  filename)))
(GtkImage 'add-method-slot! 'set_from_pixbuf (lambda (self resend  pixbuf) 
	(gtk-image-set-from-pixbuf (self 'this)  pixbuf)))
(GtkImage 'add-method-slot! 'set_from_stock (lambda (self resend  stock_id size) 
	(gtk-image-set-from-stock (self 'this)  stock_id size)))
(GtkImage 'add-method-slot! 'set_from_icon_set (lambda (self resend  icon_set size) 
	(gtk-image-set-from-icon-set (self 'this)  icon_set size)))
(GtkImage 'add-method-slot! 'set_from_animation (lambda (self resend  animation) 
	(gtk-image-set-from-animation (self 'this)  animation)))
(GtkImage 'add-method-slot! 'set_from_icon_name (lambda (self resend  icon_name size) 
	(gtk-image-set-from-icon-name (self 'this)  icon_name size)))
(GtkImage 'add-method-slot! 'set_pixel_size (lambda (self resend  pixel_size) 
	(gtk-image-set-pixel-size (self 'this)  pixel_size)))
(GtkImage 'add-method-slot! 'get_storage_type (lambda (self resend ) 
	(gtk-image-get-storage-type (self 'this) )))
(GtkImage 'add-method-slot! 'get_pixmap (lambda (self resend  pixmap mask) 
	(gtk-image-get-pixmap (self 'this)  pixmap mask)))
(GtkImage 'add-method-slot! 'get_image (lambda (self resend  gdk_image mask) 
	(gtk-image-get-image (self 'this)  gdk_image mask)))
(GtkImage 'add-method-slot! 'get_pixbuf (lambda (self resend ) 
	(gtk-image-get-pixbuf (self 'this) )))
(GtkImage 'add-method-slot! 'get_stock (lambda (self resend  stock_id size) 
	(gtk-image-get-stock (self 'this)  stock_id size)))
(GtkImage 'add-method-slot! 'get_icon_set (lambda (self resend  icon_set size) 
	(gtk-image-get-icon-set (self 'this)  icon_set size)))
(GtkImage 'add-method-slot! 'get_animation (lambda (self resend ) 
	(gtk-image-get-animation (self 'this) )))
(GtkImage 'add-method-slot! 'get_icon_name (lambda (self resend  icon_name size) 
	(gtk-image-get-icon-name (self 'this)  icon_name size)))
(GtkImage 'add-method-slot! 'get_pixel_size (lambda (self resend ) 
	(gtk-image-get-pixel-size (self 'this) )))
(GtkImage 'add-method-slot! 'set (lambda (self resend  val mask) 
	(gtk-image-set (self 'this)  val mask)))
(GtkImage 'add-method-slot! 'get (lambda (self resend  val mask) 
	(gtk-image-get (self 'this)  val mask)))
(GtkImage 'add-method-slot! 'new (lambda (self resend ) 
	(self 'set-this! (gtk-image-new ))))
