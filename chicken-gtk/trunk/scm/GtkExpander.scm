;**********************************************
;DO NOT MODIFY GENERATED BY make-proto-tree.scm
(declare (unit GtkExpander))
(define GtkExpander (GtkBin 'clone))
(GtkExpander 'add-method-slot! 'set_expanded (lambda (self resend  expanded) 
	(gtk-expander-set-expanded (self 'this)  expanded)))
(GtkExpander 'add-method-slot! 'get_expanded (lambda (self resend ) 
	(gtk-expander-get-expanded (self 'this) )))
(GtkExpander 'add-method-slot! 'set_spacing (lambda (self resend  spacing) 
	(gtk-expander-set-spacing (self 'this)  spacing)))
(GtkExpander 'add-method-slot! 'get_spacing (lambda (self resend ) 
	(gtk-expander-get-spacing (self 'this) )))
(GtkExpander 'add-method-slot! 'set_label (lambda (self resend  label) 
	(gtk-expander-set-label (self 'this)  label)))
(GtkExpander 'add-method-slot! 'get_label (lambda (self resend ) 
	(gtk-expander-get-label (self 'this) )))
(GtkExpander 'add-method-slot! 'set_use_underline (lambda (self resend  use_underline) 
	(gtk-expander-set-use-underline (self 'this)  use_underline)))
(GtkExpander 'add-method-slot! 'get_use_underline (lambda (self resend ) 
	(gtk-expander-get-use-underline (self 'this) )))
(GtkExpander 'add-method-slot! 'set_use_markup (lambda (self resend  use_markup) 
	(gtk-expander-set-use-markup (self 'this)  use_markup)))
(GtkExpander 'add-method-slot! 'get_use_markup (lambda (self resend ) 
	(gtk-expander-get-use-markup (self 'this) )))
(GtkExpander 'add-method-slot! 'set_label_widget (lambda (self resend  label_widget) 
	(gtk-expander-set-label-widget (self 'this)  label_widget)))
(GtkExpander 'add-method-slot! 'get_label_widget (lambda (self resend ) 
	(gtk-expander-get-label-widget (self 'this) )))
(GtkExpander 'add-method-slot! 'new (lambda (self resend  label) 
	(self 'set-this! (gtk-expander-new  label))))
