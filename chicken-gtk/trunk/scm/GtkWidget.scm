;**********************************************
;DO NOT MODIFY GENERATED BY make-proto-tree.scm
(declare (unit GtkWidget))
(define GtkWidget (GtkObject 'clone))
(GtkWidget 'add-method-slot! 'ref (lambda (self resend ) 
	(gtk-widget-ref (self 'this) )))
(GtkWidget 'add-method-slot! 'unref (lambda (self resend ) 
	(gtk-widget-unref (self 'this) )))
(GtkWidget 'add-method-slot! 'destroy (lambda (self resend ) 
	(gtk-widget-destroy (self 'this) )))
(GtkWidget 'add-method-slot! 'destroyed (lambda (self resend  widget_pointer) 
	(gtk-widget-destroyed (self 'this)  widget_pointer)))
(GtkWidget 'add-method-slot! 'set (lambda (self resend  first_property_name) 
	(gtk-widget-set (self 'this)  first_property_name)))
(GtkWidget 'add-method-slot! 'unparent (lambda (self resend ) 
	(gtk-widget-unparent (self 'this) )))
(GtkWidget 'add-method-slot! 'show (lambda (self resend ) 
	(gtk-widget-show (self 'this) )))
(GtkWidget 'add-method-slot! 'show_now (lambda (self resend ) 
	(gtk-widget-show-now (self 'this) )))
(GtkWidget 'add-method-slot! 'hide (lambda (self resend ) 
	(gtk-widget-hide (self 'this) )))
(GtkWidget 'add-method-slot! 'show_all (lambda (self resend ) 
	(gtk-widget-show-all (self 'this) )))
(GtkWidget 'add-method-slot! 'hide_all (lambda (self resend ) 
	(gtk-widget-hide-all (self 'this) )))
(GtkWidget 'add-method-slot! 'set_no_show_all (lambda (self resend  no_show_all) 
	(gtk-widget-set-no-show-all (self 'this)  no_show_all)))
(GtkWidget 'add-method-slot! 'get_no_show_all (lambda (self resend ) 
	(gtk-widget-get-no-show-all (self 'this) )))
(GtkWidget 'add-method-slot! 'map-- (lambda (self resend ) 
	(gtk-widget-map (self 'this) )))
(GtkWidget 'add-method-slot! 'unmap (lambda (self resend ) 
	(gtk-widget-unmap (self 'this) )))
(GtkWidget 'add-method-slot! 'realize (lambda (self resend ) 
	(gtk-widget-realize (self 'this) )))
(GtkWidget 'add-method-slot! 'unrealize (lambda (self resend ) 
	(gtk-widget-unrealize (self 'this) )))
(GtkWidget 'add-method-slot! 'queue_draw (lambda (self resend ) 
	(gtk-widget-queue-draw (self 'this) )))
(GtkWidget 'add-method-slot! 'queue_draw_area (lambda (self resend  x y width height) 
	(gtk-widget-queue-draw-area (self 'this)  x y width height)))
(GtkWidget 'add-method-slot! 'queue_clear (lambda (self resend ) 
	(gtk-widget-queue-clear (self 'this) )))
(GtkWidget 'add-method-slot! 'queue_clear_area (lambda (self resend  x y width height) 
	(gtk-widget-queue-clear-area (self 'this)  x y width height)))
(GtkWidget 'add-method-slot! 'queue_resize (lambda (self resend ) 
	(gtk-widget-queue-resize (self 'this) )))
(GtkWidget 'add-method-slot! 'queue_resize_no_redraw (lambda (self resend ) 
	(gtk-widget-queue-resize-no-redraw (self 'this) )))
(GtkWidget 'add-method-slot! 'draw (lambda (self resend  area) 
	(gtk-widget-draw (self 'this)  area)))
(GtkWidget 'add-method-slot! 'size_request (lambda (self resend  requisition) 
	(gtk-widget-size-request (self 'this)  requisition)))
(GtkWidget 'add-method-slot! 'size_allocate (lambda (self resend  allocation) 
	(gtk-widget-size-allocate (self 'this)  allocation)))
(GtkWidget 'add-method-slot! 'get_child_requisition (lambda (self resend  requisition) 
	(gtk-widget-get-child-requisition (self 'this)  requisition)))
(GtkWidget 'add-method-slot! 'add_accelerator (lambda (self resend  accel_signal accel_group accel_key accel_mods accel_flags) 
	(gtk-widget-add-accelerator (self 'this)  accel_signal accel_group accel_key accel_mods accel_flags)))
(GtkWidget 'add-method-slot! 'remove_accelerator (lambda (self resend  accel_group accel_key accel_mods) 
	(gtk-widget-remove-accelerator (self 'this)  accel_group accel_key accel_mods)))
(GtkWidget 'add-method-slot! 'set_accel_path (lambda (self resend  accel_path accel_group) 
	(gtk-widget-set-accel-path (self 'this)  accel_path accel_group)))
(GtkWidget 'add-method-slot! 'list_accel_closures (lambda (self resend ) 
	(gtk-widget-list-accel-closures (self 'this) )))
(GtkWidget 'add-method-slot! 'can_activate_accel (lambda (self resend  signal_id) 
	(gtk-widget-can-activate-accel (self 'this)  signal_id)))
(GtkWidget 'add-method-slot! 'mnemonic_activate (lambda (self resend  group_cycling) 
	(gtk-widget-mnemonic-activate (self 'this)  group_cycling)))
(GtkWidget 'add-method-slot! 'event (lambda (self resend  event) 
	(gtk-widget-event (self 'this)  event)))
(GtkWidget 'add-method-slot! 'send_expose (lambda (self resend  event) 
	(gtk-widget-send-expose (self 'this)  event)))
(GtkWidget 'add-method-slot! 'activate (lambda (self resend ) 
	(gtk-widget-activate (self 'this) )))
(GtkWidget 'add-method-slot! 'set_scroll_adjustments (lambda (self resend  hadjustment vadjustment) 
	(gtk-widget-set-scroll-adjustments (self 'this)  hadjustment vadjustment)))
(GtkWidget 'add-method-slot! 'reparent (lambda (self resend  new_parent) 
	(gtk-widget-reparent (self 'this)  new_parent)))
(GtkWidget 'add-method-slot! 'intersect (lambda (self resend  area intersection) 
	(gtk-widget-intersect (self 'this)  area intersection)))
(GtkWidget 'add-method-slot! 'region_intersect (lambda (self resend  region) 
	(gtk-widget-region-intersect (self 'this)  region)))
(GtkWidget 'add-method-slot! 'freeze_child_notify (lambda (self resend ) 
	(gtk-widget-freeze-child-notify (self 'this) )))
(GtkWidget 'add-method-slot! 'child_notify (lambda (self resend  child_property) 
	(gtk-widget-child-notify (self 'this)  child_property)))
(GtkWidget 'add-method-slot! 'thaw_child_notify (lambda (self resend ) 
	(gtk-widget-thaw-child-notify (self 'this) )))
(GtkWidget 'add-method-slot! 'is_focus (lambda (self resend ) 
	(gtk-widget-is-focus (self 'this) )))
(GtkWidget 'add-method-slot! 'grab_focus (lambda (self resend ) 
	(gtk-widget-grab-focus (self 'this) )))
(GtkWidget 'add-method-slot! 'grab_default (lambda (self resend ) 
	(gtk-widget-grab-default (self 'this) )))
(GtkWidget 'add-method-slot! 'set_name (lambda (self resend  name) 
	(gtk-widget-set-name (self 'this)  name)))
(GtkWidget 'add-method-slot! 'get_name (lambda (self resend ) 
	(gtk-widget-get-name (self 'this) )))
(GtkWidget 'add-method-slot! 'set_state (lambda (self resend  state) 
	(gtk-widget-set-state (self 'this)  state)))
(GtkWidget 'add-method-slot! 'set_sensitive (lambda (self resend  sensitive) 
	(gtk-widget-set-sensitive (self 'this)  sensitive)))
(GtkWidget 'add-method-slot! 'set_app_paintable (lambda (self resend  app_paintable) 
	(gtk-widget-set-app-paintable (self 'this)  app_paintable)))
(GtkWidget 'add-method-slot! 'set_double_buffered (lambda (self resend  double_buffered) 
	(gtk-widget-set-double-buffered (self 'this)  double_buffered)))
(GtkWidget 'add-method-slot! 'set_redraw_on_allocate (lambda (self resend  redraw_on_allocate) 
	(gtk-widget-set-redraw-on-allocate (self 'this)  redraw_on_allocate)))
(GtkWidget 'add-method-slot! 'set_parent (lambda (self resend  parent) 
	(gtk-widget-set-parent (self 'this)  parent)))
(GtkWidget 'add-method-slot! 'set_parent_window (lambda (self resend  parent_window) 
	(gtk-widget-set-parent-window (self 'this)  parent_window)))
(GtkWidget 'add-method-slot! 'set_child_visible (lambda (self resend  is_visible) 
	(gtk-widget-set-child-visible (self 'this)  is_visible)))
(GtkWidget 'add-method-slot! 'get_child_visible (lambda (self resend ) 
	(gtk-widget-get-child-visible (self 'this) )))
(GtkWidget 'add-method-slot! 'get_parent (lambda (self resend ) 
	(gtk-widget-get-parent (self 'this) )))
(GtkWidget 'add-method-slot! 'get_parent_window (lambda (self resend ) 
	(gtk-widget-get-parent-window (self 'this) )))
(GtkWidget 'add-method-slot! 'child_focus (lambda (self resend  direction) 
	(gtk-widget-child-focus (self 'this)  direction)))
(GtkWidget 'add-method-slot! 'set_size_request (lambda (self resend  width height) 
	(gtk-widget-set-size-request (self 'this)  width height)))
(GtkWidget 'add-method-slot! 'get_size_request (lambda (self resend  width height) 
	(gtk-widget-get-size-request (self 'this)  width height)))
(GtkWidget 'add-method-slot! 'set_uposition (lambda (self resend  x y) 
	(gtk-widget-set-uposition (self 'this)  x y)))
(GtkWidget 'add-method-slot! 'set_usize (lambda (self resend  width height) 
	(gtk-widget-set-usize (self 'this)  width height)))
(GtkWidget 'add-method-slot! 'set_events (lambda (self resend  events) 
	(gtk-widget-set-events (self 'this)  events)))
(GtkWidget 'add-method-slot! 'add_events (lambda (self resend  events) 
	(gtk-widget-add-events (self 'this)  events)))
(GtkWidget 'add-method-slot! 'set_extension_events (lambda (self resend  mode) 
	(gtk-widget-set-extension-events (self 'this)  mode)))
(GtkWidget 'add-method-slot! 'get_extension_events (lambda (self resend ) 
	(gtk-widget-get-extension-events (self 'this) )))
(GtkWidget 'add-method-slot! 'get_toplevel (lambda (self resend ) 
	(gtk-widget-get-toplevel (self 'this) )))
(GtkWidget 'add-method-slot! 'get_ancestor (lambda (self resend  widget_type) 
	(gtk-widget-get-ancestor (self 'this)  widget_type)))
(GtkWidget 'add-method-slot! 'get_colormap (lambda (self resend ) 
	(gtk-widget-get-colormap (self 'this) )))
(GtkWidget 'add-method-slot! 'get_visual (lambda (self resend ) 
	(gtk-widget-get-visual (self 'this) )))
(GtkWidget 'add-method-slot! 'get_screen (lambda (self resend ) 
	(gtk-widget-get-screen (self 'this) )))
(GtkWidget 'add-method-slot! 'has_screen (lambda (self resend ) 
	(gtk-widget-has-screen (self 'this) )))
(GtkWidget 'add-method-slot! 'get_display (lambda (self resend ) 
	(gtk-widget-get-display (self 'this) )))
(GtkWidget 'add-method-slot! 'get_root_window (lambda (self resend ) 
	(gtk-widget-get-root-window (self 'this) )))
(GtkWidget 'add-method-slot! 'get_settings (lambda (self resend ) 
	(gtk-widget-get-settings (self 'this) )))
(GtkWidget 'add-method-slot! 'get_clipboard (lambda (self resend  selection) 
	(gtk-widget-get-clipboard (self 'this)  selection)))
(GtkWidget 'add-method-slot! 'get_accessible (lambda (self resend ) 
	(gtk-widget-get-accessible (self 'this) )))
(GtkWidget 'add-method-slot! 'set_colormap (lambda (self resend  colormap) 
	(gtk-widget-set-colormap (self 'this)  colormap)))
(GtkWidget 'add-method-slot! 'get_events (lambda (self resend ) 
	(gtk-widget-get-events (self 'this) )))
(GtkWidget 'add-method-slot! 'get_pointer (lambda (self resend  x y) 
	(gtk-widget-get-pointer (self 'this)  x y)))
(GtkWidget 'add-method-slot! 'is_ancestor (lambda (self resend  ancestor) 
	(gtk-widget-is-ancestor (self 'this)  ancestor)))
(GtkWidget 'add-method-slot! 'translate_coordinates (lambda (self resend  dest_widget src_x src_y dest_x dest_y) 
	(gtk-widget-translate-coordinates (self 'this)  dest_widget src_x src_y dest_x dest_y)))
(GtkWidget 'add-method-slot! 'hide_on_delete (lambda (self resend ) 
	(gtk-widget-hide-on-delete (self 'this) )))
(GtkWidget 'add-method-slot! 'set_style (lambda (self resend  style) 
	(gtk-widget-set-style (self 'this)  style)))
(GtkWidget 'add-method-slot! 'ensure_style (lambda (self resend ) 
	(gtk-widget-ensure-style (self 'this) )))
(GtkWidget 'add-method-slot! 'get_style (lambda (self resend ) 
	(gtk-widget-get-style (self 'this) )))
(GtkWidget 'add-method-slot! 'modify_style (lambda (self resend  style) 
	(gtk-widget-modify-style (self 'this)  style)))
(GtkWidget 'add-method-slot! 'get_modifier_style (lambda (self resend ) 
	(gtk-widget-get-modifier-style (self 'this) )))
(GtkWidget 'add-method-slot! 'modify_fg (lambda (self resend  state color) 
	(gtk-widget-modify-fg (self 'this)  state color)))
(GtkWidget 'add-method-slot! 'modify_bg (lambda (self resend  state color) 
	(gtk-widget-modify-bg (self 'this)  state color)))
(GtkWidget 'add-method-slot! 'modify_text (lambda (self resend  state color) 
	(gtk-widget-modify-text (self 'this)  state color)))
(GtkWidget 'add-method-slot! 'modify_base (lambda (self resend  state color) 
	(gtk-widget-modify-base (self 'this)  state color)))
(GtkWidget 'add-method-slot! 'modify_font (lambda (self resend  font_desc) 
	(gtk-widget-modify-font (self 'this)  font_desc)))
(GtkWidget 'add-method-slot! 'create_pango_context (lambda (self resend ) 
	(gtk-widget-create-pango-context (self 'this) )))
(GtkWidget 'add-method-slot! 'get_pango_context (lambda (self resend ) 
	(gtk-widget-get-pango-context (self 'this) )))
(GtkWidget 'add-method-slot! 'create_pango_layout (lambda (self resend  text) 
	(gtk-widget-create-pango-layout (self 'this)  text)))
(GtkWidget 'add-method-slot! 'render_icon (lambda (self resend  stock_id size detail) 
	(gtk-widget-render-icon (self 'this)  stock_id size detail)))
(GtkWidget 'add-method-slot! 'set_composite_name (lambda (self resend  name) 
	(gtk-widget-set-composite-name (self 'this)  name)))
(GtkWidget 'add-method-slot! 'get_composite_name (lambda (self resend ) 
	(gtk-widget-get-composite-name (self 'this) )))
(GtkWidget 'add-method-slot! 'reset_rc_styles (lambda (self resend ) 
	(gtk-widget-reset-rc-styles (self 'this) )))
(GtkWidget 'add-method-slot! 'style_get_property (lambda (self resend  property_name value) 
	(gtk-widget-style-get-property (self 'this)  property_name value)))
(GtkWidget 'add-method-slot! 'style_get_valist (lambda (self resend  first_property_name var_args) 
	(gtk-widget-style-get-valist (self 'this)  first_property_name var_args)))
(GtkWidget 'add-method-slot! 'style_get (lambda (self resend  first_property_name) 
	(gtk-widget-style-get (self 'this)  first_property_name)))
(GtkWidget 'add-method-slot! 'set_direction (lambda (self resend  dir) 
	(gtk-widget-set-direction (self 'this)  dir)))
(GtkWidget 'add-method-slot! 'get_direction (lambda (self resend ) 
	(gtk-widget-get-direction (self 'this) )))
(GtkWidget 'add-method-slot! 'shape_combine_mask (lambda (self resend  shape_mask offset_x offset_y) 
	(gtk-widget-shape-combine-mask (self 'this)  shape_mask offset_x offset_y)))
(GtkWidget 'add-method-slot! 'reset_shapes (lambda (self resend ) 
	(gtk-widget-reset-shapes (self 'this) )))
(GtkWidget 'add-method-slot! 'path (lambda (self resend  path_length path path_reversed) 
	(gtk-widget-path (self 'this)  path_length path path_reversed)))
(GtkWidget 'add-method-slot! 'class_path (lambda (self resend  path_length path path_reversed) 
	(gtk-widget-class-path (self 'this)  path_length path path_reversed)))
(GtkWidget 'add-method-slot! 'list_mnemonic_labels (lambda (self resend ) 
	(gtk-widget-list-mnemonic-labels (self 'this) )))
(GtkWidget 'add-method-slot! 'add_mnemonic_label (lambda (self resend  label) 
	(gtk-widget-add-mnemonic-label (self 'this)  label)))
(GtkWidget 'add-method-slot! 'remove_mnemonic_label (lambda (self resend  label) 
	(gtk-widget-remove-mnemonic-label (self 'this)  label)))
(GtkWidget 'add-method-slot! 'new (lambda (self resend  type first_property_name) 
	(self 'set-this! (gtk-widget-new  type first_property_name))))
