;**********************************************
;DO NOT MODIFY GENERATED BY make-proto-tree.scm
(declare (unit GtkCTree))
(define GtkCTree (GtkCList 'clone))
(GtkCTree 'add-method-slot! 'insert_node (lambda (self resend  parent sibling text spacing pixmap_closed mask_closed pixmap_opened mask_opened is_leaf expanded) 
	(gtk-ctree-insert-node (self 'this)  parent sibling text spacing pixmap_closed mask_closed pixmap_opened mask_opened is_leaf expanded)))
(GtkCTree 'add-method-slot! 'remove_node (lambda (self resend  node) 
	(gtk-ctree-remove-node (self 'this)  node)))
(GtkCTree 'add-method-slot! 'insert_gnode (lambda (self resend  parent sibling gnode func data) 
	(gtk-ctree-insert-gnode (self 'this)  parent sibling gnode func data)))
(GtkCTree 'add-method-slot! 'export_to_gnode (lambda (self resend  parent sibling node func data) 
	(gtk-ctree-export-to-gnode (self 'this)  parent sibling node func data)))
(GtkCTree 'add-method-slot! 'post_recursive (lambda (self resend  node func data) 
	(gtk-ctree-post-recursive (self 'this)  node func data)))
(GtkCTree 'add-method-slot! 'post_recursive_to_depth (lambda (self resend  node depth func data) 
	(gtk-ctree-post-recursive-to-depth (self 'this)  node depth func data)))
(GtkCTree 'add-method-slot! 'pre_recursive (lambda (self resend  node func data) 
	(gtk-ctree-pre-recursive (self 'this)  node func data)))
(GtkCTree 'add-method-slot! 'pre_recursive_to_depth (lambda (self resend  node depth func data) 
	(gtk-ctree-pre-recursive-to-depth (self 'this)  node depth func data)))
(GtkCTree 'add-method-slot! 'is_viewable (lambda (self resend  node) 
	(gtk-ctree-is-viewable (self 'this)  node)))
(GtkCTree 'add-method-slot! 'last (lambda (self resend  node) 
	(gtk-ctree-last (self 'this)  node)))
(GtkCTree 'add-method-slot! 'find_node_ptr (lambda (self resend  ctree_row) 
	(gtk-ctree-find-node-ptr (self 'this)  ctree_row)))
(GtkCTree 'add-method-slot! 'node_nth (lambda (self resend  row) 
	(gtk-ctree-node-nth (self 'this)  row)))
(GtkCTree 'add-method-slot! 'find (lambda (self resend  node child) 
	(gtk-ctree-find (self 'this)  node child)))
(GtkCTree 'add-method-slot! 'is_ancestor (lambda (self resend  node child) 
	(gtk-ctree-is-ancestor (self 'this)  node child)))
(GtkCTree 'add-method-slot! 'find_by_row_data (lambda (self resend  node data) 
	(gtk-ctree-find-by-row-data (self 'this)  node data)))
(GtkCTree 'add-method-slot! 'find_all_by_row_data (lambda (self resend  node data) 
	(gtk-ctree-find-all-by-row-data (self 'this)  node data)))
(GtkCTree 'add-method-slot! 'find_by_row_data_custom (lambda (self resend  node data func) 
	(gtk-ctree-find-by-row-data-custom (self 'this)  node data func)))
(GtkCTree 'add-method-slot! 'find_all_by_row_data_custom (lambda (self resend  node data func) 
	(gtk-ctree-find-all-by-row-data-custom (self 'this)  node data func)))
(GtkCTree 'add-method-slot! 'is_hot_spot (lambda (self resend  x y) 
	(gtk-ctree-is-hot-spot (self 'this)  x y)))
(GtkCTree 'add-method-slot! 'move (lambda (self resend  node new_parent new_sibling) 
	(gtk-ctree-move (self 'this)  node new_parent new_sibling)))
(GtkCTree 'add-method-slot! 'expand (lambda (self resend  node) 
	(gtk-ctree-expand (self 'this)  node)))
(GtkCTree 'add-method-slot! 'expand_recursive (lambda (self resend  node) 
	(gtk-ctree-expand-recursive (self 'this)  node)))
(GtkCTree 'add-method-slot! 'expand_to_depth (lambda (self resend  node depth) 
	(gtk-ctree-expand-to-depth (self 'this)  node depth)))
(GtkCTree 'add-method-slot! 'collapse (lambda (self resend  node) 
	(gtk-ctree-collapse (self 'this)  node)))
(GtkCTree 'add-method-slot! 'collapse_recursive (lambda (self resend  node) 
	(gtk-ctree-collapse-recursive (self 'this)  node)))
(GtkCTree 'add-method-slot! 'collapse_to_depth (lambda (self resend  node depth) 
	(gtk-ctree-collapse-to-depth (self 'this)  node depth)))
(GtkCTree 'add-method-slot! 'toggle_expansion (lambda (self resend  node) 
	(gtk-ctree-toggle-expansion (self 'this)  node)))
(GtkCTree 'add-method-slot! 'toggle_expansion_recursive (lambda (self resend  node) 
	(gtk-ctree-toggle-expansion-recursive (self 'this)  node)))
(GtkCTree 'add-method-slot! 'select (lambda (self resend  node) 
	(gtk-ctree-select (self 'this)  node)))
(GtkCTree 'add-method-slot! 'select_recursive (lambda (self resend  node) 
	(gtk-ctree-select-recursive (self 'this)  node)))
(GtkCTree 'add-method-slot! 'unselect (lambda (self resend  node) 
	(gtk-ctree-unselect (self 'this)  node)))
(GtkCTree 'add-method-slot! 'unselect_recursive (lambda (self resend  node) 
	(gtk-ctree-unselect-recursive (self 'this)  node)))
(GtkCTree 'add-method-slot! 'real_select_recursive (lambda (self resend  node state) 
	(gtk-ctree-real-select-recursive (self 'this)  node state)))
(GtkCTree 'add-method-slot! 'node_set_text (lambda (self resend  node column text) 
	(gtk-ctree-node-set-text (self 'this)  node column text)))
(GtkCTree 'add-method-slot! 'node_set_pixmap (lambda (self resend  node column pixmap mask) 
	(gtk-ctree-node-set-pixmap (self 'this)  node column pixmap mask)))
(GtkCTree 'add-method-slot! 'node_set_pixtext (lambda (self resend  node column text spacing pixmap mask) 
	(gtk-ctree-node-set-pixtext (self 'this)  node column text spacing pixmap mask)))
(GtkCTree 'add-method-slot! 'set_node_info (lambda (self resend  node text spacing pixmap_closed mask_closed pixmap_opened mask_opened is_leaf expanded) 
	(gtk-ctree-set-node-info (self 'this)  node text spacing pixmap_closed mask_closed pixmap_opened mask_opened is_leaf expanded)))
(GtkCTree 'add-method-slot! 'node_set_shift (lambda (self resend  node column vertical horizontal) 
	(gtk-ctree-node-set-shift (self 'this)  node column vertical horizontal)))
(GtkCTree 'add-method-slot! 'node_set_selectable (lambda (self resend  node selectable) 
	(gtk-ctree-node-set-selectable (self 'this)  node selectable)))
(GtkCTree 'add-method-slot! 'node_get_selectable (lambda (self resend  node) 
	(gtk-ctree-node-get-selectable (self 'this)  node)))
(GtkCTree 'add-method-slot! 'node_get_cell_type (lambda (self resend  node column) 
	(gtk-ctree-node-get-cell-type (self 'this)  node column)))
(GtkCTree 'add-method-slot! 'node_get_text (lambda (self resend  node column text) 
	(gtk-ctree-node-get-text (self 'this)  node column text)))
(GtkCTree 'add-method-slot! 'node_get_pixmap (lambda (self resend  node column pixmap mask) 
	(gtk-ctree-node-get-pixmap (self 'this)  node column pixmap mask)))
(GtkCTree 'add-method-slot! 'node_get_pixtext (lambda (self resend  node column text spacing pixmap mask) 
	(gtk-ctree-node-get-pixtext (self 'this)  node column text spacing pixmap mask)))
(GtkCTree 'add-method-slot! 'get_node_info (lambda (self resend  node text spacing pixmap_closed mask_closed pixmap_opened mask_opened is_leaf expanded) 
	(gtk-ctree-get-node-info (self 'this)  node text spacing pixmap_closed mask_closed pixmap_opened mask_opened is_leaf expanded)))
(GtkCTree 'add-method-slot! 'node_set_row_style (lambda (self resend  node style) 
	(gtk-ctree-node-set-row-style (self 'this)  node style)))
(GtkCTree 'add-method-slot! 'node_get_row_style (lambda (self resend  node) 
	(gtk-ctree-node-get-row-style (self 'this)  node)))
(GtkCTree 'add-method-slot! 'node_set_cell_style (lambda (self resend  node column style) 
	(gtk-ctree-node-set-cell-style (self 'this)  node column style)))
(GtkCTree 'add-method-slot! 'node_get_cell_style (lambda (self resend  node column) 
	(gtk-ctree-node-get-cell-style (self 'this)  node column)))
(GtkCTree 'add-method-slot! 'node_set_foreground (lambda (self resend  node color) 
	(gtk-ctree-node-set-foreground (self 'this)  node color)))
(GtkCTree 'add-method-slot! 'node_set_background (lambda (self resend  node color) 
	(gtk-ctree-node-set-background (self 'this)  node color)))
(GtkCTree 'add-method-slot! 'node_set_row_data (lambda (self resend  node data) 
	(gtk-ctree-node-set-row-data (self 'this)  node data)))
(GtkCTree 'add-method-slot! 'node_set_row_data_full (lambda (self resend  node data destroy) 
	(gtk-ctree-node-set-row-data-full (self 'this)  node data destroy)))
(GtkCTree 'add-method-slot! 'node_get_row_data (lambda (self resend  node) 
	(gtk-ctree-node-get-row-data (self 'this)  node)))
(GtkCTree 'add-method-slot! 'node_moveto (lambda (self resend  node column row_align col_align) 
	(gtk-ctree-node-moveto (self 'this)  node column row_align col_align)))
(GtkCTree 'add-method-slot! 'node_is_visible (lambda (self resend  node) 
	(gtk-ctree-node-is-visible (self 'this)  node)))
(GtkCTree 'add-method-slot! 'set_indent (lambda (self resend  indent) 
	(gtk-ctree-set-indent (self 'this)  indent)))
(GtkCTree 'add-method-slot! 'set_spacing (lambda (self resend  spacing) 
	(gtk-ctree-set-spacing (self 'this)  spacing)))
(GtkCTree 'add-method-slot! 'set_show_stub (lambda (self resend  show_stub) 
	(gtk-ctree-set-show-stub (self 'this)  show_stub)))
(GtkCTree 'add-method-slot! 'set_line_style (lambda (self resend  line_style) 
	(gtk-ctree-set-line-style (self 'this)  line_style)))
(GtkCTree 'add-method-slot! 'set_expander_style (lambda (self resend  expander_style) 
	(gtk-ctree-set-expander-style (self 'this)  expander_style)))
(GtkCTree 'add-method-slot! 'set_drag_compare_func (lambda (self resend  cmp_func) 
	(gtk-ctree-set-drag-compare-func (self 'this)  cmp_func)))
(GtkCTree 'add-method-slot! 'sort_node (lambda (self resend  node) 
	(gtk-ctree-sort-node (self 'this)  node)))
(GtkCTree 'add-method-slot! 'sort_recursive (lambda (self resend  node) 
	(gtk-ctree-sort-recursive (self 'this)  node)))
