;**********************************************
;DO NOT MODIFY GENERATED BY make-proto-tree.scm
(declare (unit GtkEntry))
(define GtkEntry (GtkWidget 'clone))
(GtkEntry 'add-method-slot! 'set_visibility (lambda (self resend  visible) 
	(gtk-entry-set-visibility (self 'this)  visible)))
(GtkEntry 'add-method-slot! 'get_visibility (lambda (self resend ) 
	(gtk-entry-get-visibility (self 'this) )))
(GtkEntry 'add-method-slot! 'set_invisible_char (lambda (self resend  ch) 
	(gtk-entry-set-invisible-char (self 'this)  ch)))
(GtkEntry 'add-method-slot! 'get_invisible_char (lambda (self resend ) 
	(gtk-entry-get-invisible-char (self 'this) )))
(GtkEntry 'add-method-slot! 'set_has_frame (lambda (self resend  setting) 
	(gtk-entry-set-has-frame (self 'this)  setting)))
(GtkEntry 'add-method-slot! 'get_has_frame (lambda (self resend ) 
	(gtk-entry-get-has-frame (self 'this) )))
(GtkEntry 'add-method-slot! 'set_max_length (lambda (self resend  max) 
	(gtk-entry-set-max-length (self 'this)  max)))
(GtkEntry 'add-method-slot! 'get_max_length (lambda (self resend ) 
	(gtk-entry-get-max-length (self 'this) )))
(GtkEntry 'add-method-slot! 'set_activates_default (lambda (self resend  setting) 
	(gtk-entry-set-activates-default (self 'this)  setting)))
(GtkEntry 'add-method-slot! 'get_activates_default (lambda (self resend ) 
	(gtk-entry-get-activates-default (self 'this) )))
(GtkEntry 'add-method-slot! 'set_width_chars (lambda (self resend  n_chars) 
	(gtk-entry-set-width-chars (self 'this)  n_chars)))
(GtkEntry 'add-method-slot! 'get_width_chars (lambda (self resend ) 
	(gtk-entry-get-width-chars (self 'this) )))
(GtkEntry 'add-method-slot! 'set_text (lambda (self resend  text) 
	(gtk-entry-set-text (self 'this)  text)))
(GtkEntry 'add-method-slot! 'get_text (lambda (self resend ) 
	(gtk-entry-get-text (self 'this) )))
(GtkEntry 'add-method-slot! 'get_layout (lambda (self resend ) 
	(gtk-entry-get-layout (self 'this) )))
(GtkEntry 'add-method-slot! 'get_layout_offsets (lambda (self resend  x y) 
	(gtk-entry-get-layout-offsets (self 'this)  x y)))
(GtkEntry 'add-method-slot! 'set_alignment (lambda (self resend  xalign) 
	(gtk-entry-set-alignment (self 'this)  xalign)))
(GtkEntry 'add-method-slot! 'get_alignment (lambda (self resend ) 
	(gtk-entry-get-alignment (self 'this) )))
(GtkEntry 'add-method-slot! 'set_completion (lambda (self resend  completion) 
	(gtk-entry-set-completion (self 'this)  completion)))
(GtkEntry 'add-method-slot! 'get_completion (lambda (self resend ) 
	(gtk-entry-get-completion (self 'this) )))
(GtkEntry 'add-method-slot! 'layout_index_to_text_index (lambda (self resend  layout_index) 
	(gtk-entry-layout-index-to-text-index (self 'this)  layout_index)))
(GtkEntry 'add-method-slot! 'text_index_to_layout_index (lambda (self resend  text_index) 
	(gtk-entry-text-index-to-layout-index (self 'this)  text_index)))
(GtkEntry 'add-method-slot! 'append_text (lambda (self resend  text) 
	(gtk-entry-append-text (self 'this)  text)))
(GtkEntry 'add-method-slot! 'prepend_text (lambda (self resend  text) 
	(gtk-entry-prepend-text (self 'this)  text)))
(GtkEntry 'add-method-slot! 'set_position (lambda (self resend  position) 
	(gtk-entry-set-position (self 'this)  position)))
(GtkEntry 'add-method-slot! 'select_region (lambda (self resend  start end) 
	(gtk-entry-select-region (self 'this)  start end)))
(GtkEntry 'add-method-slot! 'set_editable (lambda (self resend  editable) 
	(gtk-entry-set-editable (self 'this)  editable)))
(GtkEntry 'add-method-slot! 'new (lambda (self resend ) 
	(self 'set-this! (gtk-entry-new ))))
